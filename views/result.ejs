<!DOCTYPE html>
<html lang="en">
  <head>
    <title>SafetyPro: Results</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <header>
        <h1>SafetyPro</h1>
        <h2>Results</h2>
    </header>
    <section>
        <p>
           <img src="<%= img%>" alt="uploaded image" />
        </p>
        <p id="results-count"><%= count%> stop signs</p>
        <p id="results-intro"><%= resultsintro%></p>
        <div id="results-found">
            <ul>
                <% for(var i=0; i < objectsFound.length; i++) { %>
                    <li><%= objectsFound[i].object%> (<%= objectsFound[i].confidence.toFixed(2)*100 %>%) </li>
                <% } %>
            </ul>
        </div>
    </section>
    <section>
        <form action="/" enctype="multipart/form-data" method="post">
            <input type="file" name="file-to-upload" />
            <input type="submit" value="Upload" />
        </form>
    </section>
  </body>
</html>
