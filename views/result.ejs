<!DOCTYPE html>
<html lang="en">
  <head>
    <title>SafetyPro: Results</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <header>
        <p class="logo"><a href="/">SafetyPro</a></p>
        <p class="tagline">Are there stop signs in your image?</p>
        <h1>Results</h1>
    </header>
    <section>
        <p>
           <img src="<%= img%>" alt="uploaded image" />
        </p>
        <p id="results-count"><%= count%> stop signs</p>
        <p id="results-intro"><%= resultsintro%></p>
        <div id="results-found">
            <ul>
                <% for(var i=0; i < objectsFound.length; i++) { %>
                    <li><%= objectsFound[i].object%> (<%= objectsFound[i].confidence.toFixed(2)*100 %> % confidence) </li>
                <% } %>
            </ul>
        </div>
    </section>
    <section>
        <h3>Upload an image and SafetyPro will analyze it:</h3>
        <form id="upload" class="upload" action="/" enctype="multipart/form-data" method="post">
            <div>
                <p>
                    <input type="file" name="file-to-upload">
                </p>
            </div>
            <div>
                <p>
                    <input type="submit" value="Upload">
                </p>
            </div>
        </form>
    </section>
  </body>
</html>
